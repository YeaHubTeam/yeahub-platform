import { State } from '@/shared/config/store/State';

export const getProfilePageState = (state: State) => state.profilePage;
export const getProfilePageProfileId = (state: State) => state.profilePage.profileId;
